const CACHE='perla-b2b-v244';const STATIC=['/','/manifest.webmanifest','/icon.svg','/badge.svg'];self.addEventListener('install',e=>{e.waitUntil(caches.open(CACHE).then(c=>c.addAll(STATIC)).catch(()=>null));self.skipWaiting()});self.addEventListener('activate',e=>{e.waitUntil(Promise.all([caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))),self.clients.claim()]))});self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const u=new URL(e.request.url);if(u.pathname.startsWith('/api/'))return;e.respondWith(fetch(e.request).catch(()=>caches.match(e.request).then(r=>r||caches.match('/'))))});self.addEventListener('push',e=>{let d={};try{d=e.data?e.data.json():{}}catch(_){d={body:e.data?e.data.text():'Nova mensagem recebida.'}}e.waitUntil(self.registration.showNotification(d.title||'Perla Andina',{body:d.body||'Nova mensagem recebida.',icon:'/icon.svg',badge:'/badge.svg',tag:d.tag||'perla-b2b',data:{openUrl:d.openUrl||'/'}}))});self.addEventListener('notificationclick',e=>{e.notification.close();const u=(e.notification.data&&e.notification.data.openUrl)||'/';e.waitUntil(self.clients.openWindow(u))});
