const {VERSION,backendUrl}=require('./_backend');module.exports=async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({ok:false,error:'METHOD_NOT_ALLOWED'});const pushConfigured=Boolean(String(process.env.VAPID_PUBLIC_KEY||'').trim()&&String(process.env.VAPID_PRIVATE_KEY||'').trim()&&String(process.env.PUSH_SERVER_TOKEN||'').trim());const backend=backendUrl();return res.status(200).json({ok:true,version:VERSION,portal:'perlaandinacatalogo',frontend:'vercel-shell',backendConfigured:!!backend,backend:backend||'',pushMode:'integrated-fail-soft',pushConfigured})};
