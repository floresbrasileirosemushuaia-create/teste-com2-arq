const {VERSION,backendUrl}=require('./_backend');
function allowed(raw){try{const u=new URL(String(raw||''));return u.protocol==='https:'&&u.hostname==='script.google.com'&&/^\/macros\/s\/[A-Za-z0-9_-]+\/exec$/.test(u.pathname)}catch(_){return false}}
module.exports=async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({ok:false,error:'METHOD_NOT_ALLOWED'});try{const body=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{}),BACKEND=backendUrl(),postUrl=String(body.postUrl||BACKEND),subscription=body.subscription||null;if(!BACKEND)return res.status(503).json({ok:false,error:'B2B_BACKEND_NOT_CONFIGURED',version:VERSION});if(!allowed(postUrl)||!body.enrollmentToken||!subscription||!subscription.endpoint||!subscription.keys)return res.status(400).json({ok:false,error:'INVALID_REGISTRATION'});const form=new URLSearchParams();form.set('action','pushRegister');form.set('enrollmentToken',String(body.enrollmentToken));form.set('subscription',JSON.stringify(subscription));form.set('deviceId',String(body.deviceId||''));form.set('portalDeviceId',String(body.portalDeviceId||''));form.set('userAgent',String(body.userAgent||'').slice(0,700));form.set('platform',String(body.platform||'').slice(0,160));const r=await fetch(postUrl,{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded;charset=UTF-8'},body:form.toString(),redirect:'follow'}),text=await r.text();if(!r.ok||!/^OK\b/.test(text.trim()))return res.status(502).json({ok:false,error:'APPS_SCRIPT_REJECTED',detail:text.slice(0,400)});return res.status(200).json({ok:true,version:VERSION,deviceId:String(body.deviceId||'')})}catch(err){return res.status(500).json({ok:false,error:'REGISTER_FAILED',detail:String(err&&err.message||err).slice(0,500)})}};
