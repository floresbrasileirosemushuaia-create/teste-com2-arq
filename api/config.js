const PUSH_GATEWAY='https://perla-andina-b2b-push.vercel.app';
module.exports=async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({ok:false,error:'METHOD_NOT_ALLOWED'});try{const r=await fetch(PUSH_GATEWAY+'/api/config',{cache:'no-store'}),out=await r.json();if(!r.ok||!out||!out.vapidPublicKey)return res.status(503).json({ok:false,configured:false,error:'PUSH_GATEWAY_NOT_READY'});return res.status(200).json({ok:true,configured:true,version:'2.3.0',vapidPublicKey:out.vapidPublicKey})}catch(err){return res.status(503).json({ok:false,configured:false,error:'PUSH_CONFIG_FAILED',detail:String(err&&err.message||err)})}};
