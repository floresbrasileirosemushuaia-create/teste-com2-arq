const {VERSION}=require('./_backend');module.exports=async function handler(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET')return res.status(405).json({ok:false,error:'METHOD_NOT_ALLOWED'});const key=String(process.env.VAPID_PUBLIC_KEY||'').trim();if(!key)return res.status(503).json({ok:false,configured:false,error:'VAPID_PUBLIC_KEY_MISSING',version:VERSION});return res.status(200).json({ok:true,configured:true,version:VERSION,vapidPublicKey:key})};
